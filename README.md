# MemeVote - On-chain Голосование за Мемы

Децентрализованное приложение (dApp) для голосования за мемы на базе смарт-контрактов ink! в экосистеме Polkadot.

## Описание

MemeVote - это демонстрационный проект, который показывает возможности разработки смарт-контрактов на языке ink! (Rust) для Substrate-based блокчейнов. Пользователи могут добавлять мемы (URL изображения + заголовок) и голосовать за понравившиеся мемы. Все данные хранятся on-chain в смарт-контракте.

## Основные возможности

- Добавление мемов с заголовком и URL изображения
- Голосование (лайки) за мемы
- Защита от повторного голосования
- Просмотр всех мемов и топа по лайкам
- Интеграция с Polkadot.js Extension для работы с кошельками

## Технологический стек

### Smart Contract
- **ink! 5.0.0** - фреймворк для написания смарт-контрактов на Rust
- **Rust** - язык программирования
- **cargo-contract** - инструмент для сборки и развертывания контрактов

### Frontend
- **React 18** - фреймворк для UI
- **Polkadot.js API** - библиотека для взаимодействия с блокчейном
- **Polkadot.js Extension** - расширение браузера для управления кошельками

### Поддерживаемые сети
- Local contracts-node (для разработки)
- Aleph Zero Testnet
- Astar Shibuya (testnet)
- Любая Substrate-based сеть с поддержкой pallet-contracts

## Структура проекта

```
polkadot-meme-vote-ink/
├── contract/              # Смарт-контракт на ink!
│   ├── lib.rs            # Основной код контракта
│   ├── Cargo.toml        # Зависимости Rust
│   └── .gitignore
├── frontend/             # React приложение
│   ├── public/
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   │   ├── MemeForm.js
│   │   │   ├── MemeList.js
│   │   │   └── MemeCard.js
│   │   ├── contract/     # Метаданные контракта
│   │   │   └── metadata.json
│   │   ├── App.js        # Главный компонент
│   │   ├── index.js
│   │   └── index.css
│   ├── package.json
│   └── .gitignore
└── README.md
```

## Установка и запуск

### Предварительные требования

1. **Rust и cargo** (для сборки контракта)
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup default stable
rustup update
rustup component add rust-src
```

2. **cargo-contract** (инструмент для работы с ink!)
```bash
cargo install cargo-contract --force
```

3. **Node.js и npm** (для фронтенда)
```bash
# Установите Node.js v16+ с официального сайта
# https://nodejs.org/
```

4. **contracts-node** (локальная тестовая сеть, опционально)
```bash
cargo install contracts-node --git https://github.com/paritytech/substrate-contracts-node.git
```

5. **Polkadot.js Extension**
   - Установите расширение для [Chrome/Brave](https://chrome.google.com/webstore/detail/polkadot%7Bjs%7D-extension/mopnmbcafieddcagagdcbnhejhlodfdd) или [Firefox](https://addons.mozilla.org/en-US/firefox/addon/polkadot-js-extension/)

### Сборка смарт-контракта

```bash
cd contract

# Сборка контракта
cargo contract build

# После сборки в директории target/ink появятся файлы:
# - meme_vote.contract (для развертывания)
# - meme_vote.wasm (WebAssembly байткод)
# - metadata.json (метаданные контракта)
```

### Запуск тестов контракта

```bash
cd contract

# Запуск unit тестов
cargo test

# Или через cargo-contract
cargo contract test
```

### Развертывание контракта

#### Вариант 1: Локальная сеть (contracts-node)

```bash
# Запустите локальную ноду в отдельном терминале
contracts-node --dev

# В другом терминале разверните контракт через Polkadot.js Apps UI:
# 1. Откройте https://contracts-ui.substrate.io/
# 2. Подключитесь к Local Node (ws://127.0.0.1:9944)
# 3. Нажмите "Add New Contract" -> "Upload New Contract Code"
# 4. Выберите файл target/ink/meme_vote.contract
# 5. Выберите конструктор "new" или "default"
# 6. Нажмите "Next" и "Deploy"
# 7. Скопируйте адрес контракта
```

#### Вариант 2: Через cargo-contract (CLI)

```bash
cd contract

# Развертывание на локальной ноде
cargo contract instantiate \
  --constructor new \
  --suri //Alice \
  --url ws://127.0.0.1:9944

# Сохраните адрес контракта из вывода
```

#### Вариант 3: Testnet (например, Aleph Zero)

```bash
# Используйте Contracts UI или cargo-contract с параметром --url
cargo contract instantiate \
  --constructor new \
  --suri "ваша seed фраза" \
  --url wss://ws.test.azero.dev
```

### Настройка и запуск фронтенда

```bash
cd frontend

# Установите зависимости
npm install

# Настройте подключение к контракту
# Отредактируйте файл src/App.js:
# - CONTRACT_ADDRESS: адрес развернутого контракта
# - WS_PROVIDER: WebSocket endpoint вашей сети

# Примеры endpoints:
# - Local: ws://127.0.0.1:9944
# - Aleph Zero Testnet: wss://ws.test.azero.dev
# - Astar Shibuya: wss://rpc.shibuya.astar.network

# Скопируйте metadata.json из контракта
cp ../contract/target/ink/metadata.json src/contract/metadata.json

# Запустите приложение
npm start

# Приложение откроется на http://localhost:3000
```

## Использование приложения

1. **Подключение кошелька**
   - Убедитесь, что Polkadot.js Extension установлено
   - При первом запуске приложение запросит доступ к расширению
   - Выберите аккаунт из выпадающего списка

2. **Добавление мема**
   - Введите заголовок (макс. 100 символов)
   - Введите URL изображения (прямая ссылка на картинку)
   - Нажмите "Добавить мем"
   - Подтвердите транзакцию в Polkadot.js Extension

3. **Голосование**
   - Просмотрите список мемов
   - Нажмите кнопку "👍 Лайк" на понравившемся меме
   - Подтвердите транзакцию
   - Вы можете проголосовать за каждый мем только один раз

4. **Просмотр топа**
   - Переключитесь на вкладку "Топ мемов"
   - Увидите мемы, отсортированные по количеству лайков

## Функции смарт-контракта

### Структура данных

```rust
pub struct Meme {
    pub id: u32,
    pub creator: AccountId,
    pub title: String,
    pub url: String,
    pub likes: u32,
}
```

### Методы контракта

#### `add_meme(title: String, url: String) -> Result<u32>`
Добавляет новый мем в контракт.

**Проверки:**
- Заголовок не длиннее 100 символов
- URL не пустой

**Возвращает:** ID нового мема

**События:** `MemeAdded(id, creator)`

#### `vote_up(meme_id: u32) -> Result<()>`
Голосует за мем (добавляет лайк).

**Проверки:**
- Мем существует
- Пользователь еще не голосовал за этот мем

**События:** `Voted(meme_id, voter)`

#### `get_meme(meme_id: u32) -> Option<Meme>`
Возвращает мем по ID.

#### `get_memes(from: u32, limit: u32) -> Vec<Meme>`
Возвращает список мемов с пагинацией.

**Параметры:**
- `from` - начальный ID
- `limit` - максимальное количество мемов

#### `get_top_memes(limit: u32) -> Vec<Meme>`
Возвращает топ мемов по количеству лайков.

**Параметры:**
- `limit` - количество мемов в топе

#### `has_voted(account: AccountId, meme_id: u32) -> bool`
Проверяет, голосовал ли аккаунт за мем.

#### `total_memes() -> u32`
Возвращает общее количество мемов.

## Тестирование

### Unit тесты контракта

Контракт включает комплексные unit-тесты:

```bash
cd contract
cargo test
```

**Покрытые сценарии:**
- Создание нового контракта
- Добавление мема
- Валидация длины заголовка (>100 символов)
- Валидация пустого URL
- Голосование за мем
- Защита от повторного голосования
- Попытка голосования за несуществующий мем
- Получение списка мемов
- Получение топа мемов

## Примеры использования

### Пример взаимодействия с контрактом через код

```javascript
// Добавление мема
await contract.tx
  .addMeme(
    { gasLimit },
    "Смешной кот",
    "https://example.com/cat.jpg"
  )
  .signAndSend(account, { signer });

// Голосование
await contract.tx
  .voteUp({ gasLimit }, memeId)
  .signAndSend(account, { signer });

// Получение мемов
const { result } = await contract.query.getMemes(
  account,
  { gasLimit },
  1,   // from
  10   // limit
);
```

## Возможные улучшения

- Возможность удаления мемов создателем
- Модерация контента
- Категории мемов
- Комментарии к мемам
- Награды токенами за популярные мемы
- IPFS интеграция для хранения изображений
- Более эффективный алгоритм сортировки для топа
- Кеширование данных на фронтенде
- Пагинация с бесконечной прокруткой
- Фильтрация и поиск мемов

## Безопасность

⚠️ **Это демонстрационный проект для обучения!**

Не используйте в production без:
- Аудита кода контракта
- Тестирования на testnet
- Реализации дополнительных проверок безопасности
- Ограничений на размер данных
- Защиты от спама

## Лицензия

MIT License

## Полезные ссылки

- [ink! Documentation](https://use.ink/)
- [Polkadot.js API Docs](https://polkadot.js.org/docs/)
- [Substrate Contracts](https://docs.substrate.io/tutorials/smart-contracts/)
- [Aleph Zero](https://alephzero.org/)
- [Astar Network](https://astar.network/)

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию ink! и Polkadot.js
2. Убедитесь что все зависимости установлены корректно
3. Проверьте консоль браузера на ошибки
4. Убедитесь что контракт развернут и адрес указан верно

---

**Создано для демонстрации возможностей разработки смарт-контрактов на ink! в экосистеме Polkadot.**